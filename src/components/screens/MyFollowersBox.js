import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class MyFollowersBox extends Component {
    render() {
        return (
            <View>
                <Text> Follower box </Text>
            </View>
        )
    }
}

const styles = StyleSheet.create({})
